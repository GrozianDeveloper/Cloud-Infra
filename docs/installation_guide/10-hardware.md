# Hardware

Fill [instance/hardware.md](../../instance/hardware.md). No secrets.

## Ask

CPU, RAM, NICs (MAC, LAN subnet, gateway), disks (size, type, serial, transport), GPU (passthrough? to which VM/apps).

## Effect

Pool layout ([13](13-pools-datasets.md)). GPU → FileFlows / HandBrake on the TrueNAS guest.

Template: [instance/_template/hardware.md](../../instance/_template/hardware.md).
